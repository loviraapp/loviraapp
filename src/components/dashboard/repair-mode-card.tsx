"use client";

export function RepairModeCard() {
  return (
    <section className="rounded-2xl border border-dashed border-primary/25 bg-primary-soft/40 px-4 py-4">
      <p className="text-sm font-medium text-foreground">Repair mode</p>
      <p className="mt-1 text-sm text-muted">
        If today felt tense, try reconnecting before solving.
      </p>
    </section>
  );
}
