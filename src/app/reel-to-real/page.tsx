import { ReelToRealFlow } from "@/components/reel-to-real/reel-to-real-flow";

export const metadata = {
  title: "Reel to Real",
  description:
    "Turn shared reels into warm conversation and real connection together.",
};

export default function ReelToRealPage() {
  return <ReelToRealFlow />;
}
