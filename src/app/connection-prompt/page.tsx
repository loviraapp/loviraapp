import { ConnectionPromptScreen } from "@/components/connection-prompt/connection-prompt-screen";

export const metadata = {
  title: "Connection Prompt",
  description: "One warm question to open a real conversation together.",
};

export default function ConnectionPromptPage() {
  return <ConnectionPromptScreen />;
}
